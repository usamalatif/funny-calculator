// Must exactly match the non-consumable "Remove Ads" product configured
// in App Store Connect for this app's bundle ID before purchases will work.
export const REMOVE_ADS_SKU = 'com.weiblocks.calculatude.removeads';
